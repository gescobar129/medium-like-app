<%= form_for [@article, @article.comments.build], url: comments_path do |f| %>
   <p>
       <%= f.label :title %>:
       <%= f.text_field :title %><br />
   </p>
   <p>
       <%= f.label :body %>:
       <%= f.text_area :body %><br />
   </p>
   <p>
       <%= f.submit %>
   </p>
<% end %>